## 1. 会话持久化基础

- [x] 1.1 新增独立运行时会话 SQLite 的路径配置、schema version、迁移命令及 `conversations`、`messages`、`message_evidence`、`turn_requests` 表
- [x] 1.2 实现会话 repository，覆盖按客户端摘要创建、分页列表、读取、删除、消息顺序和证据快照事务写入
- [x] 1.3 为会话库启用外键、WAL、超时 pending 收敛和健康检查，并验证会话库故障不影响只读知识库接口
- [x] 1.4 增加会话 schema 迁移、事务回滚、客户端隔离和级联删除的后端单元测试

## 2. 会话历史与流式问答 API

- [x] 2.1 定义并校验 `X-BGP-Client-ID`、会话摘要、消息、同步状态和游标分页 DTO
- [x] 2.2 实现会话创建、列表、详情和删除接口，并统一处理客户端不匹配的未找到响应
- [x] 2.3 实现带 `request_id` 的会话 turn 流式接口，在最终事件前持久化回答、终态和证据，重试不得产生重复消息
- [x] 2.4 实现停止、异常和模型不可用时的轮次终态写入及中断恢复行为
- [x] 2.5 实现旧本地 v2 单会话的一次性幂等导入接口
- [x] 2.6 增加 CRUD、分页、幂等重试、流式完成、中止、异常和旧会话导入的 API 测试

## 3. 结构化行内引用与证据详情

- [x] 3.1 为本轮 context pack 分配稳定 citation 标识，并把允许标识和受控引用格式加入回答生成契约
- [x] 3.2 实现回答引用解析与 allowlist 校验，返回兼容 `answer`、结构化 `answer_parts` 和 `inline_citation_status`
- [x] 3.3 丰富 citation DTO，写入 `citation_id`、chunk、来源、section、完整句摘录、上下文快照和知识库 `release_id`
- [x] 3.4 实现消息作用域内的证据详情接口，默认返回相关章节，并支持受大小限制的分页或分 section 完整文档读取
- [x] 3.5 对证据详情实施来源标识校验和路径边界检查，拒绝跨消息 citation、任意文件路径和发布根目录越界
- [x] 3.6 增加合法引用、未知引用、无行内引用、跨轮次读取、完整句边界、发布版本差异和路径越界测试

## 4. 真正的模型与 SSE 流式输出

- [x] 4.1 为 DeepSeek 回答实现 `stream: true` 上游适配器，解析内容增量、结束原因、usage、UTF-8 分片和错误帧
- [x] 4.2 为召回、精排、上下文组装、模型首字、生成、持久化和总流程增加单调时钟计时及统一 timings DTO
- [x] 4.3 扩展服务端 SSE 协议，发送带递增 sequence 的 stage started/complete、answer_delta、citation_delta、heartbeat、done 和 error 事件
- [x] 4.4 实现跨上游 chunk 的增量 citation 控制标记解析，确保半个或非法标记不会泄露到用户正文
- [x] 4.5 让无状态回答流和会话 turn 流复用同一生成器，在停止、断线或异常时保存部分正文、已有引用和阶段耗时
- [x] 4.6 增加首个增量先于 done、阶段事件顺序、耗时约束、Unicode 分片、跨分片 citation、心跳、停止、异常和 buffered 降级的后端测试

## 5. 前端流式回答与耗时体验

- [x] 5.1 扩展 `rag-stream` 事件类型和解析器，分别回调阶段、答案增量、引用增量、心跳和最终快照，并按 sequence 去重
- [x] 5.2 重构助手消息状态，分离阶段进度、流式正文、结构化引用、最终快照和生成终态，不再用阶段文字覆盖正文
- [x] 5.3 实现进行中的阶段时间线与本地经过计时，完成后使用服务端 timings 展示召回、精排、首字、生成和总计用时
- [x] 5.4 按 animation frame 或短时间窗批量提交增量；仅在用户接近底部时自动跟随，并提供“回到最新”入口
- [x] 5.5 在停止、断线和恢复时保留部分答案，使用相同 `request_id` 与 sequence 恢复，并以 done 最终快照校正内容
- [x] 5.6 增加真实渐进显示、阶段计时、sequence 去重、批量渲染、滚动脱离、停止保留部分正文和屏幕阅读器播报测试

## 6. 前端会话历史体验

- [x] 6.1 扩展前端类型和 API client，支持会话摘要、会话详情、同步状态及带会话流式请求
- [x] 6.2 重构 `ChatShell` 状态，使活动会话、活动助手消息、活动 citation 和未同步请求相互独立
- [x] 6.3 将侧栏实现为历史会话列表，支持确定性标题、新建、切换、分页加载和确认删除；新建不得清除旧会话
- [x] 6.4 实现会话首次恢复、发送后的乐观渲染、服务端确认、失败缓存和相同 `request_id` 重试
- [x] 6.5 实现旧 `bgp-chat-conversation` v2 本地数据的一次性导入，确认成功前保留原数据并展示同步状态
- [x] 6.6 增加历史排序、刷新恢复、切换隔离、新建保留、删除确认、离线失败和旧数据迁移的前端测试

## 7. 消息复制修复

- [x] 7.1 实现消息纯文本序列化，将段落、列表和行内 citation 转为可读文本并排除内部标识与隐藏上下文
- [x] 7.2 实现 Clipboard API 主路径及临时 `textarea` 兼容回退，捕获权限和浏览器异常并完整清理临时状态
- [x] 7.3 为每条消息增加“已复制”或“复制失败”的短时反馈、`aria-live` 播报和键盘焦点样式
- [x] 7.4 增加标准复制、权限拒绝、API 缺失、回退失败、带引用回答和复制不改变会话状态的测试

## 8. 行内引用与本轮证据交互

- [x] 8.1 扩展消息渲染器以消费流式 citation 和最终 `answer_parts`，渲染带编号、描述性标签和可见焦点的内部证据按钮
- [x] 8.2 实现引用点击状态机：选择所属助手消息、打开桌面面板或移动抽屉、展开文档、滚动并高亮目标 citation
- [x] 8.3 重构 `CitationPanel`，按活动 citation 懒加载相关章节，显示完整句摘录，并提供分页或分 section 的完整文档查看入口
- [x] 8.4 实现证据详情缓存、加载骨架、失败重试、历史证据快照和 release 版本差异提示
- [x] 8.5 保留外部来源链接作为次级入口，并确保关闭面板、返回回答和键盘焦点恢复行为一致
- [x] 8.6 增加当前轮定位、历史轮隔离、流式引用、移动抽屉、文档懒加载、ROA 截断复现、加载失败和键盘操作测试

## 9. 集成验收与部署准备

- [x] 9.1 运行后端完整测试，验证发布知识库仍以只读方式工作且旧 RAG 接口响应兼容
- [x] 9.2 运行前端类型检查、lint、单元和集成测试，确认桌面与移动布局契约不回退
- [x] 9.3 增加端到端验收：创建两个会话、刷新恢复、切换历史、查看逐步生成与各阶段耗时、复制成功、点击上一轮引用并在对应“本轮证据”中显示文档内容
- [x] 9.4 使用慢速测试 Provider 验证至少两个可见答案增量先于 `done` 到达，并核对召回、精排、首字、生成和总耗时关系
- [x] 9.5 使用包含 `called Rou…` 截断问题的 RPKI 问答做人工验收，确认流式引用可点击且能看到含 `Route Origin Authorization (ROA)` 的完整句子
- [x] 9.6 更新中文架构、接口、运维、会话库备份与回滚文档，并补充 `BGP_CHAT_DB_PATH`、SSE 代理缓冲和超时配置且不记录凭据
- [x] 9.7 在远端部署前备份现有版本和会话数据路径，部署后验证 screen、39280/39281、首字时间、增量到达、阶段耗时、历史恢复、复制和证据定位，失败时按设计回滚前端且保留独立会话库
