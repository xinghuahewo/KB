## 1. 私有运行环境与版本基线

- [x] 1.1 盘点目标 GPU、驱动、CUDA 与显存，确定并记录兼容的 Python、Docling 和模型版本矩阵
- [x] 1.2 新增锁定依赖、GPU 容器、模型 manifest、SBOM 和许可证清单生成入口
- [x] 1.3 先以失败测试定义离线启动、模型 hash、GPU 预检和禁止运行时下载边界
- [x] 1.4 构建私有镜像并在断网环境完成 Docling fixture 冒烟验证

## 2. Canonical Block v2 与 Docling 适配

- [x] 2.1 新增 Docling 清洗配置及 parsed document、Block、表格、图片资产和追溯 Schema
- [x] 2.2 先以失败测试定义稳定 Block ID、父子结构、阅读顺序、bbox、特殊内容与原子输出
- [x] 2.3 实现 Docling 输出到项目 Canonical Block v2 的适配器和固定 fixture
- [x] 2.4 实现 Docling 默认路由与现有解析器显式 fallback，阻止未审核 fallback 发布

## 3. 文档预检与自适应 OCR

- [x] 3.1 先以失败测试定义格式、页数、文本覆盖、图片比例、加密状态和输入指纹预检
- [x] 3.2 实现数字原生、扫描页、图片区域和低质量页的 OCR 路由策略
- [x] 3.3 并行保存原生文本与 OCR 证据，记录触发原因、语言、引擎和置信度
- [x] 3.4 覆盖损坏 PDF、加密 PDF、空页面、中英混排和低置信 OCR 故障用例

## 4. 清洗规则与转换治理

- [x] 4.1 新增清洗规则、transformation、人工决策和 Block 审核状态 Schema
- [x] 4.2 先以失败测试定义无损规范化、结构校正和语义性修改的权限边界
- [x] 4.3 实现 Unicode、换行、空白、页眉页脚、标题、跨页段落和跨页表格规则
- [x] 4.4 实现逐次 transformation 审计和原始 Block 永久保留
- [x] 4.5 实现 pending、fallback、低置信 OCR 与冲突 Block 的复核队列和下游隔离

## 5. 可恢复批处理

- [x] 5.1 新增 cleaning run、document status、错误、重试和性能记录 Schema
- [x] 5.2 先以失败测试定义状态机、处理指纹、幂等跳过、配置失效和断点续跑
- [x] 5.3 实现文档级批处理编排、临时目录、原子发布和单篇失败隔离
- [x] 5.4 实现 OOM、超时和暂态模型错误的有限重试，以及内容/Schema 错误直接隔离
- [x] 5.5 生成批次中文报告和吞吐、p50/p95、GPU 显存、OCR、fallback 汇总

## 6. v2 派生与差异评测

- [x] 6.1 从 approved cleaned Block 生成 Markdown v2、assets v2 和 chunks v2
- [x] 6.2 先以失败测试定义未批准 Block 排除、来源引用保持和派生稳定性
- [x] 6.3 实现逐文档 v1/v2 正文、标题、section、表格、图片、chunk 和来源引用差异
- [x] 6.4 实现正文覆盖率、不可归因删除、fallback 和重复运行稳定性门禁

## 7. 全量迁移与人工验收

- [x] 7.1 全量处理现有 54 篇语料，确保每篇达到 approved 或 quarantined 终态
- [x] 7.2 建立约 12 篇高风险人工验收集、标注说明和评测数据集
- [x] 7.3 计算标题层级 F1、阅读顺序、表格结构和 OCR CER，并达到规格门槛
- [x] 7.4 处理全部阻断差异、低置信度、fallback 与隔离文档，生成迁移报告

## 8. 发布切换与项目集成

- [x] 8.1 新增版本化语料发布指针、显式切换命令和经过验证的 v1 回滚命令
- [x] 8.2 先以失败测试定义未通过门禁禁止切换、原子切换、下游 v2 读取和回滚行为
- [x] 8.3 接入质量检查、制品 producer、主流水线、项目索引和阶段验收 gate
- [x] 8.4 运行切换前全量测试、离线容器测试、全量迁移验收和敏感信息检查
- [x] 8.5 显式切换到 v2，重建发布物并验证下游；随后执行一次 v1 回滚演练并恢复 v2
- [x] 8.6 完成 OpenSpec 严格校验、工作树检查和最终中文验收证据
